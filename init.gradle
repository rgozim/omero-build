settingsEvaluated {
    it.pluginManagement {
        repositories {
            maven {
                url System.getenv("MVN_REPOSITORY_URL") ?: 'https://artifacts.openmicroscopy.org/artifactory/maven/'
            }
            gradlePluginPortal()
        }
    }
}
